import React from 'react';
import StyledContainer from './styleContainer';

import Logo from '../logo';
import Nav from '../nav';

export default () => {
    return (
        <StyledContainer>
            <Logo />
            <Nav />
        </StyledContainer>
    )
}